{
  "name": "my-enigma-machine",
  "version": "0.5.0",
  "engines": {
    "node": ">=10.4.0"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "gts": "^3.1.0",
    "minimist": "^1.2.5",
    "yaml": "^1.10.2"
  },
  "scripts": {
    "start": "tsc && node build/main.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
