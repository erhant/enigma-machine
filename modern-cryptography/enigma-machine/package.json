{
  "devDependencies": {
    "typescript": "^4.0.3",
    "gts": "^3.1.0",
    "@types/node": "^14.11.2"
  },
  "dependencies": {
    "gts": "^3.1.0"
  },
  "scripts": {
    "start": "tsc && node build/main.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
